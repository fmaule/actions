name: publish-cloudflare-worker
description: publish-cloudflare-worker action
inputs:
  foo:
    description: foo
    required: false
outputs:
  bar:
    description: bar
runs:
  using: node16
  main: dist/index.js
